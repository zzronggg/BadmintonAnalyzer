<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Top bar -->
    <style name="top_app_bar_yellow" parent="Theme.MaterialComponents.Light.NoActionBar">
        <item name="android:windowBackground">@color/white</item>
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/egg_yellow</item>
        <item name="colorPrimaryVariant">@color/login_btn</item>
        <item name="colorOnPrimary">@color/black</item>
        <item name="android:actionMenuTextAppearance">@style/menu_text_style</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
    </style>



    <!--  Text Style   -->
    <style name="menu_text_style" parent="@android:style/TextAppearance.DeviceDefault.Widget.ActionBar.Menu">
        <item name="android:textSize">20sp</item>
        <item name="android:textColor">@color/black</item>
        <item name="android:textAllCaps">false</item>
    </style>

</resources>